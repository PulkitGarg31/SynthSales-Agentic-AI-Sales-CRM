# Web framework
fastapi
uvicorn[standard]
python-multipart>=0.0.31   # <0.0.30 had multipart-parsing DoS advisories

# Database
sqlalchemy>=2.0
psycopg[binary]
alembic

# Config & validation
pydantic
pydantic-settings>=2.14.2  # GHSA-4xgf-cpjx-pc3j
email-validator

# Security minimums for transitive deps flagged by pip-audit (patched versions).
# Pinned explicitly so the deploy build can't resolve back to a vulnerable one.
starlette>=1.3.1           # PYSEC-2026-248 / -249 (also constrains FastAPI's Starlette)
cryptography>=48.0.1       # GHSA-537c-gmf6-5ccf
httplib2>=0.32.0           # PYSEC-2026-3444 (via google-auth-httplib2)
pyasn1>=0.6.4              # PYSEC-2026-3455/-3456/-3457 (via google-auth)

# Auth
pyjwt
passlib

# Background jobs + shared abuse-control state
apscheduler
redis                  # optional rate-limiter backend (REDIS_URL); in-memory fallback

# HTTP
httpx

# Integrations (providers)
# AI (Gemini/Groq/OpenRouter) + email verification (Verifalia/ZeroBounce) use httpx REST — no SDKs.
ddgs                   # DuckDuckGo search (company research)
dnspython              # MX-record lookups (free email-verification layer)
google-api-python-client       # Gmail + Calendar
google-auth-oauthlib
google-auth-httplib2
